import 'package:flutter/material.dart';

class center1 extends StatelessWidget{
  const center1({super.key});
  @override
  Widget build(context){
    return Container(child :const Text('jlezjz'));
  }
}